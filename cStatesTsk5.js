//Write a function to find the largest between two numbers
const findBig = (num1, num2) => {
    if (num1 > num2) {
        console.log(`${num1} is bigger than ${num2}`);
        
        
    } else { 
        console.log(`${num2} is bigger than ${num1}`);
        
        
    }
}
findBig(55, 60)
findBig(88, 2)
findBig(2, 88)