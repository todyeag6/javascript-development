/*Write a function that takes a string as input and returns the length of the longest word in the string. 
For example, findLongestWord("The quick brown fox jumped over the lazy dog") -> "The longest word is jumped."*/

function stringLength() {
    if(typeof str !== 'string') {
        return false
    }
    
}