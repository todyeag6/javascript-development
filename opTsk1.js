/*Write a function for this task. Assign the below math problem to two different variables. Using PEMDAS

and parenthesis, update 1 or both variables to get an output answer below.

10 + 32 * 12 / 3*/

const solveMathProblem = () => {
    
    let originalExpression = 10 + 65 * 12 / 3;
    let adjustedExpression = 10 + (65 * (12 / -4)); 

    console.log("Original Expression Result:", originalExpression);
    console.log("Adjusted Expression Result:", adjustedExpression);
};


solveMathProblem();
