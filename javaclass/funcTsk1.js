/*Write a function expression for adding two strings together. This expression should take 2 parameters.

This function expression should return both strings using the + sign. Be sure to test the function

expression using a console.log statement.*/

const joinStr = function (str1, str2) {
    let joined = (str1 + str2)
    return joined
}


console.log(joinStr("Hole in", " one"));
